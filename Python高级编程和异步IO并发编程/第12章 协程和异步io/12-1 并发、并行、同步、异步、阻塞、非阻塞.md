```python
#并发与并行
#并发：一个时间段内，有几个程序在同一个CPU上运行，但是任意时刻只有一个程序在CPU上运行
#并行：任意时刻点上，有多个程序同时运行在多个CPU上，
#同步异步
#同步：是指代码调用io操作时，必须等待io操作完成才返回的调用方式
#异步：是指代码调用io操作时，不必等io操作完成就返回的调用方式

#阻塞和非阻塞
#阻塞：是指调用函数的时候当前线程被挂起
#非阻塞：是指调用函数时候当前线程不会被挂起，而是立即返回
#epoll并不代表一定比select好
#在并发搞的情况下，连接活跃度不是很高，epoll比select并发性不高，同时连接很活跃，select比epoll好
# PPT见github
